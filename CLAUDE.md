# 地球宿舍 AI 配置檔案

## ⚠️ 必須執行的檢查

**啟動時立即執行**：
1. **讀取 PROGRESS.md** - 了解當前工作狀態和進度
2. **檢查 .kiro/skills/** - 確認可用技能列表
3. **查看最近的 commit 記錄** - 了解專案最新變更

如果 PROGRESS.md 不存在，建立初始版本記錄當前狀況。

---

## 專案背景

**專案名稱**：《鬥氣割草》H5 遊戲  
**技術棧**：Phaser 3 + TypeScript + Vite  
**工作目錄**：`/mnt/d/3C/douqi`  

### 核心檔案結構
```
douqi/
├── src/           # 遊戲源碼
├── public/        # 靜態資源
├── assets/        # 遊戲素材
├── 規格書/        # 遊戲規格文檔
├── soul.md        # 專案核心文檔
├── PROGRESS.md    # 當前進度狀態
└── .kiro/skills/  # AI 技能庫
```

---

## 專案目標與方向

### 主要目標
- 開發 H5 割草遊戲 MVP
- 建立穩定的開發流程
- 整合地形編輯器功能

### 技術要求
- 保持代碼品質與規範
- 按階段完成功能模組
- 定期更新進度狀態

---

## ⚠️ 嚴禁執行的命令

**Fleet 決策：禁止阻塞性開發服務器命令**

**絕對禁止執行：**
- `npm run dev` - 啟動開發服務器
- `npm start` - 啟動應用服務器  
- `yarn dev` / `yarn start` - Yarn 開發命令
- `vite` - 直接啟動 Vite 服務器
- 任何會啟動長期運行服務的命令

**技術原因：** 這些命令會阻塞 AgEnD 實例響應，導致狀態變為 "stuck"，無法處理新任務。

**標準替代方案：**
- ✅ `npm run build` - 構建驗證  
- ✅ `npm test` - 執行測試
- ✅ 需要開發服務器時在**外部終端**手動啟動
- ✅ 使用 `curl` 或類似工具測試 API 端點

**違規後果：** 被視為嚴重操作錯誤，可能導致實例重啟或替換。

---

## AI 協作規範

### 進度管理
- **必須更新 PROGRESS.md**：每次完成重要工作後
- **狀態同步**：與其他團隊成員保持進度一致
- **分階段交付**：避免過大的功能模組

### 技能使用
- 優先使用 `.kiro/skills/` 中的標準化技能
- 特別是：progress-tracking、meeting-notes、asset-import
- 遵循既定的開發與驗證流程

### 溝通協調
- 重要決策記錄在適當文檔中
- 與 setusyoi 用戶保持定期溝通
- 協調地球宿舍團隊其他成員的工作

---

## 緊急情況處理

如果遇到以下狀況：
- **記憶丟失**：立即檢查 PROGRESS.md 了解當前狀態
- **技能失效**：檢查 `.kiro/skills/.agend-managed-skills.json`
- **專案異常**：查看最近的 Git 記錄和錯誤日志

---

*最後更新：2026-09-24*  
*版本：v1.0 - 初始配置*
